{
  "extends": [
    "config:base"
  ],
  "packageRules": [
    {
      "matchDepTypes": ["devDependencies", "dependencies"],
      "matchUpdateTypes": ["patch", "minor", "pin", "digest"],
      "groupName": "dependencies-non-major-updates"
    }
  ]
}
